"""LongMemEval scaffold for future implementation."""

from __future__ import annotations

from pathlib import Path


def fetch_longmemeval_dataset(_: Path) -> None:
    raise NotImplementedError(
        "LongMemEval download is intentionally scaffolded for v1. "
        "Implement in follow-up once source distribution flow is finalized."
    )
